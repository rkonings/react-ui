import color from './colors';

const icon = {
    default: color.gray130,
    primary: color.blue60,
    secondairy: color.orange60,
    size: {
        xs: 10,
        s: 20,
        m: 30,
        l: 40,
        xl: 50
    }
};

export default icon;
